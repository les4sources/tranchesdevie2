- content_for :title, "Modifier l'événement"
- content_for :layout, "admin"

= adm_page_header "Modifier l'événement" do
  = link_to "Parties", admin_party_events_path, class: adm_btn_class(:ghost)

= render "form"
