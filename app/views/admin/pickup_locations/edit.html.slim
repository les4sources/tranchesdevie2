- content_for :title, "Modifier le point de retrait"
- content_for :layout, "admin"

= adm_page_header "Modifier le point de retrait : #{@pickup_location.name}" do
  = link_to "Paramètres", admin_settings_path, class: adm_btn_class(:ghost)
  = link_to "Points de retrait", admin_pickup_locations_path, class: adm_btn_class(:ghost)

= render "form", pickup_location: @pickup_location
