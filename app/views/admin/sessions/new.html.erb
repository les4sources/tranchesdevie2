<% content_for :title, "Connexion admin" %>

<div class="max-w-md mx-auto mt-12">
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8">
    <h1 class="text-2xl font-bold text-gray-900 mb-6">Connexion admin</h1>

    <%= form_with url: admin_login_path, local: true do |f| %>
      <div class="mb-4">
        <%= f.label :password, "Mot de passe", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.password_field :password, 
            class: admin_input_class,
            required: true,
            autofocus: true %>
      </div>

      <div>
        <%= f.submit "Se connecter", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    <% end %>
  </div>
</div>

