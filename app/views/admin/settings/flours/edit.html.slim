- content_for :title, "Modifier la farine"
- content_for :layout, "admin"

= adm_page_header "Modifier la farine : #{@flour.name}" do
  = link_to "Paramètres", admin_settings_path, class: adm_btn_class(:ghost)
  = link_to "Farines", admin_settings_flours_path, class: adm_btn_class(:ghost)

= render "form", flour: @flour
