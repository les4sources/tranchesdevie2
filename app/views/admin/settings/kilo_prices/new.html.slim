- content_for :title, "Nouveau prix — #{@owner.name}"
- content_for :layout, "admin"

= adm_page_header "Nouveau prix — #{@owner.name}" do
  = link_to "Retour", owner_prices_path, class: adm_btn_class(:ghost)

= render "form", price: @price, url: owner_prices_path, method: :post, submit_label: "Enregistrer"
