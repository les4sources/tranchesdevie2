- content_for :title, "Modifier l'atelier"
- content_for :layout, "admin"

= adm_page_header "Modifier l'atelier" do
  = link_to "Retour aux ateliers", admin_workshops_path, class: adm_btn_class(:default)

= render "form", workshop: @workshop, artisans: @artisans, submit_label: "Enregistrer"
